# Scripts archivados

Scripts de mantenimiento one-off que ya no se ejecutan rutinariamente. Conservados por valor histórico/operativo.

- `backfill-jugaygana-userid.js` — backfill de `jugayganaUserId` en usuarios existentes
- `send-notification.js` — utilidad CLI para enviar notificaciones FCM manuales
- `sync-all-users.js` — import masivo inicial desde JUGAYGANA

Si necesitás volver a usar alguno, moverlo de vuelta a `scripts/`.
